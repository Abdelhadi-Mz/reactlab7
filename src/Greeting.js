/**
 * Composant simple utilisé pour les tests unitaires.
 * Reçoit `name` et affiche un message de salutation.
 */
function Greeting({ name }) {
  return <h1>Bonjour, {name}</h1>;
}

export default Greeting;
